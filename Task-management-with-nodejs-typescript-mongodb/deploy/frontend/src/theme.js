import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: {
    primary: { main: "#031d2a" },
    secondary: { main: "#022c3f" },
    yellow1: "#ffcd00",
    yellow2: "#fcca50",
  },
});
